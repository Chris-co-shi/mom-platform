/**
 * System 领域模型与端口边界。
 *
 * <p>Domain 必须保持框架无关，不依赖 Spring Web、MyBatis、JPA、Feign、Redis、消息、Application、
 * Web 或 Infrastructure。S13 参数与 S14 字典保持独立子域包，均不承载 IAM 或权威主数据对象。</p>
 */
package io.github.chrisshi.mom.system.domain;
