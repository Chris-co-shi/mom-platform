# P1.6 S00 凭据轮换清单

- 状态：**Pending Confirmation**
- 适用阶段：P1.6 S00
- 安全原则：凡曾以非空默认值进入公开仓库的凭据均按已泄露处理。

## 1. 当前树清理状态

当前生产配置已恢复环境中立默认值：密码类配置不再提供非空默认值，私有基础设施地址不再作为默认地址，IAM Bootstrap、Nacos Discovery 和 OTLP Export 默认关闭。正式环境必须通过受控环境变量或 Secret 注入实际配置。

本次清理只修复当前 Git 树，不改写历史，也不代表外部系统凭据已经轮换。

## 2. 外部轮换清单

| 类别 | 影响面 | 当前树 | 外部轮换状态 |
|---|---|---|---|
| PostgreSQL 应用凭据 | `mom-iam-platform/mom-iam-server/src/main/resources/application.yml`、`mom-mdm-platform/mom-mdm-server/src/main/resources/application.yml`、`mom-integration-platform/mom-integration-server/src/main/resources/application.yml` | 已移除非空默认值 | Pending Confirmation |
| Redis 访问凭据 | `mom-iam-platform/mom-iam-server/src/main/resources/application.yml` | 已移除非空默认值 | Pending Confirmation |
| Nacos 访问凭据 | `mom-gateway/src/main/resources/application.yml`、`mom-iam-platform/mom-iam-server/src/main/resources/application.yml`、`mom-mdm-platform/mom-mdm-server/src/main/resources/application.yml`、`mom-integration-platform/mom-integration-server/src/main/resources/application.yml` | 已移除非空默认值 | Pending Confirmation |
| IAM Bootstrap 临时凭据 | `mom-iam-platform/mom-iam-server/src/main/resources/application.yml` | 已移除非空默认值并恢复默认关闭 | Pending Confirmation |

轮换时不得把新值提交到仓库、Issue、PR、构建日志、审计事件或本清单。轮换完成只记录执行人、时间、目标环境和确认结果，不记录秘密值或摘要。

## 3. 已知公开历史

- 当前 P1.6 起点：`339f47c01eb8ef637f9c27b88787a6ce1c449667`。
- 更早的公开历史也曾出现环境凭据；当前树清理不能使历史中的值失效。
- 是否改写 Git 历史不属于 S00；在未获得仓库所有者明确批准前禁止 force push 或历史重写。

## 4. 阶段门禁

在全部外部轮换项确认前：

- S00 只能标记为 `Implemented / Pushed / CI Verified / Awaiting Credential Rotation`；
- 不得标记为 `Completed`；
- 不得进入 S01；
- Draft PR 不得转为 Ready 或合并。
