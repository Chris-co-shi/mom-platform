# P1.6 S00 配置凭据暴露与风险处置记录

- S00 状态：`Completed under accepted local-development risk`
- 当前代码状态：运行配置已恢复环境中立和安全默认值，测试签名材料已隔离到测试资源。
- 历史使用范围：Chris 已确认旧值仅用于无法从外网访问的本地内网开发环境。
- 人工风险裁决：Chris 接受当前不轮换凭据、不清理 Git 历史、不批量失效 Session、Refresh Token 或 Access Token。
- 保密要求：本文件不保存秘密值、片段、摘要或可重建材料。

> 本次风险接受只适用于已确认的历史本地开发使用范围，不表示未来生产环境可以使用固定默认凭据。生产环境仍必须通过 Secret、环境变量或等效受控机制显式注入凭据。

| 风险类型 | 受影响路径 | 首次可确认 Commit | 当前树 | 已确认历史范围 | Chris 决策 | Session/Token 处置 |
|---|---|---|---|---|---|---|
| PostgreSQL 默认凭据 | IAM、MDM、Integration 的 `src/main/resources/application.yml` | `941ff78352f74977cd167cf759baeb2959c3ee46` | 已清理 | 本地内网开发环境 | Accepted / No Rotation | 不执行批量失效 |
| Redis 默认凭据 | IAM `src/main/resources/application.yml` | `334522d005470c40fa3567d5ac4653aefc4a83c9` | 已清理 | 本地内网开发环境 | Accepted / No Rotation | 不执行批量失效 |
| Nacos 默认凭据 | IAM、Gateway、MDM、Integration 的 `src/main/resources/application.yml` | `1f0254725efd43a5ae7d0bbdbf71033a80e87517` | 已清理 | 本地内网开发环境 | Accepted / No Rotation | 不执行批量失效 |
| IAM Bootstrap 默认密码 | IAM `src/main/resources/application.yml` | `4ccab103fbcef0b9e7f9088136c735edbc7315ca` | 已清理且默认关闭 | 本地内网开发环境 | Accepted / No Rotation | 不执行批量失效 |
| Refresh HMAC Pepper | IAM `application.yml`、`IamSessionProperties.java` | `c2b1c1db6d253f953ccedd78721b182d32ac6226` | 已清理并改为显式注入 | 本地内网开发环境 | Accepted / No Rotation | 不失效现有 Refresh Session |
| IAM 测试签名私钥 | IAM `src/main/resources/keys/test/` 及基础配置引用 | `53d0e5c3c12a7dd7ba9dfe9bb5a043da89b38e76` | 已移至 `src/test/resources`，运行默认不允许 | 本地内网开发环境 | Accepted / No Rotation | 不失效 Session 或 Access Token |

## 风险接受边界

1. 当前不执行凭据轮换，也不验证历史值是否仍有效。
2. 当前不执行 Git 历史清理；历史记录继续保留旧值的事实已被 Chris 接受。
3. 当前不执行 Session、Refresh Token 或 Access Token 批量失效。
4. 若未来发现旧值曾用于共享、可公网访问或生产环境，本次风险裁决自动失去适用前提，必须重新评估轮换与失效范围。
5. 新的生产部署不得复用历史固定值，必须通过 Secret、环境变量或等效受控机制注入。
