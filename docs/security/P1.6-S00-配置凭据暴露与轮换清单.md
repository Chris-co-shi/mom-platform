# P1.6 配置凭据暴露与风险处置记录

- 初始 S00 状态：`Completed under accepted local-development risk`
- 当前代码要求：运行配置保持环境中立和安全默认值；所有密码、Key、Pepper 与服务 Secret 必须由环境变量、Secret 或等效受控机制注入。
- 保密要求：本文件不保存秘密值、片段、摘要或可重建材料。
- Git 历史策略：不通过 reset、rebase 或 force-push 改写长期历史；曾进入 Git 历史的值一律视为已暴露，不得继续复用。

## 1. 历史风险记录

| 风险类型 | 受影响路径 | 首次可确认 Commit | 当前树 | 已确认历史范围 | 处置 |
|---|---|---|---|---|---|
| PostgreSQL 默认凭据 | IAM、MDM、Integration 的 `src/main/resources/application.yml` | `941ff78352f74977cd167cf759baeb2959c3ee46` | 已清理 | 本地内网开发环境 | 不得用于共享、测试、预生产或生产环境 |
| Redis 默认凭据 | IAM `src/main/resources/application.yml` | `334522d005470c40fa3567d5ac4653aefc4a83c9` | 已清理 | 本地内网开发环境 | 不得继续复用 |
| Nacos 默认凭据 | IAM、Gateway、MDM、Integration 的 `src/main/resources/application.yml` | `1f0254725efd43a5ae7d0bbdbf71033a80e87517` | 已清理 | 本地内网开发环境 | 不得继续复用 |
| IAM Bootstrap 默认密码 | IAM `src/main/resources/application.yml` | `4ccab103fbcef0b9e7f9088136c735edbc7315ca` | 已清理且默认关闭 | 本地内网开发环境 | 不得继续复用 |
| Refresh HMAC Pepper | IAM `application.yml`、`IamSessionProperties.java` | `c2b1c1db6d253f953ccedd78721b182d32ac6226` | 已清理并改为显式注入 | 本地内网开发环境 | 部署时使用受控稳定 Pepper |
| IAM 测试签名私钥 | IAM `src/main/resources/keys/test/` 及基础配置引用 | `53d0e5c3c12a7dd7ba9dfe9bb5a043da89b38e76` | 已移至 `src/test/resources`，运行默认不允许 | 本地内网开发环境 | 不得用于运行环境 |

## 2. 2026-08-03 再次暴露事件

Commit `64dac9d2738c3ff2dffd6efc1b8b4b0eca21b77d` 再次把本地 PostgreSQL、Redis、Nacos 和 IAM Bootstrap 凭据配置成非空默认值，并默认开启 IAM Bootstrap。

处置结论：

1. 当前修复提交恢复空 Secret、Nacos 默认关闭、Bootstrap 默认关闭；
2. 新增的 IAM 管理员 Recovery 同样默认关闭，密码只允许通过 `IAM_ADMIN_RECOVERY_PASSWORD` 注入；
3. 该 Commit 中出现过的所有凭据均视为已暴露，禁止继续作为 admin、PostgreSQL、Redis、Nacos 或其他系统密码使用；
4. 不清理 Git 历史，不执行 force-push；历史值继续存在于 Git 历史这一事实已明确记录；
5. 本次 admin Recovery 会替换现有密码摘要、清除锁定状态，并在数据库事务提交后撤销该用户的现有 Session 与 Refresh Token；
6. 新密码不得写入仓库、日志、文档、聊天记录中的代码提交或共享配置。

## 3. 风险接受边界

1. 历史风险接受仅适用于已确认的本地开发范围，不表示固定默认凭据可以继续存在于当前代码树；
2. 新的共享、测试、预生产和生产部署必须使用 Secret、环境变量或等效受控机制；
3. 如果任何历史值曾用于可公网访问、共享或生产环境，必须立即轮换对应凭据，并重新评估 Session、Refresh Token、Access Token 与服务连接；
4. IAM 管理员恢复仅允许非 `prod`、非 `production` Profile，执行后必须关闭恢复开关并移除密码环境变量；
5. 不得通过删除、物理重建或绕过系统账号保护来恢复内置 `admin`。
