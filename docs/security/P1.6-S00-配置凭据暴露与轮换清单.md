# P1.6 S00 配置凭据暴露与轮换清单

- 记录原则：所有进入公开仓库的非空默认凭据均按已暴露处理。
- 当前代码状态：S00 已移除运行配置中的相关非空默认值，并隔离测试签名材料。
- 外部状态：`Pending Confirmation`；代码清理不等于外部凭据已轮换。
- Git 历史：仍保留旧值；本 Slice 未改写历史，历史清理尚未批准。
- 保密要求：本文件不保存秘密值、片段、摘要或可重建材料。

| 暴露类型 | 受影响路径 | 首次可确认 Commit | 当前树 | 外部系统与责任 | 轮换状态 | Session/Token 失效评估 |
|---|---|---|---|---|---|---|
| PostgreSQL 默认凭据 | IAM、MDM、Integration 的 `src/main/resources/application.yml` | `941ff78352f74977cd167cf759baeb2959c3ee46` | 已清理 | PostgreSQL 环境所有者；Chris 负责确认实际执行人 | Pending Confirmation | 否 |
| Redis 默认凭据 | IAM `src/main/resources/application.yml` | `334522d005470c40fa3567d5ac4653aefc4a83c9` | 已清理 | Redis 环境所有者；Chris 负责确认实际执行人 | Pending Confirmation | 否 |
| Nacos 默认凭据 | IAM、Gateway、MDM、Integration 的 `src/main/resources/application.yml` | `1f0254725efd43a5ae7d0bbdbf71033a80e87517` | 已清理 | Nacos 环境所有者；Chris 负责确认实际执行人 | Pending Confirmation | 否 |
| IAM Bootstrap 默认密码 | IAM `src/main/resources/application.yml` | `4ccab103fbcef0b9e7f9088136c735edbc7315ca` | 已清理且默认关闭 | IAM 环境所有者；Chris 负责确认实际执行人 | Pending Confirmation | 若该管理员已在共享环境创建，需评估改密及相关 Session 撤销 |
| Refresh HMAC Pepper | IAM `application.yml`、`IamSessionProperties.java` | `c2b1c1db6d253f953ccedd78721b182d32ac6226` | 已清理并改为显式注入 | IAM 安全责任人；Chris 负责确认实际执行人 | Pending Confirmation | 若曾用于共享环境，必须评估轮换并撤销受影响 Refresh Session |
| IAM 测试签名私钥 | IAM `src/main/resources/keys/test/` 及基础配置引用 | `53d0e5c3c12a7dd7ba9dfe9bb5a043da89b38e76` | 已移至 `src/test/resources`，运行默认不允许 | IAM 安全责任人；Chris 负责确认实际执行人 | Pending Usage Confirmation | 若曾用于共享环境，需轮换签名密钥并评估 Access Token/Session 失效 |

## 关闭门禁

1. 每类外部系统必须由 Chris 确认实际责任人和完成证据。
2. 需要 Session/Token 失效的类别必须记录影响范围和执行结果。
3. 未完成必要轮换前，P1.6 Draft PR 不得转 Ready 或合并。
4. 历史清理必须单独审批、备份并制定协作窗口；S00 不执行。
