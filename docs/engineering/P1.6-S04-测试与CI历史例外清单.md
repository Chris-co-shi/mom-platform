# P1.6 S04 测试与 CI 历史例外清单

- 状态：Current Baseline
- 盘点日期：2026-07-29

| 文件或能力 | 当前调用方与状态 | S04 处理 | 后续删除条件 |
|---|---|---|---|
| `MdmDataProbeService` / `MdmDataProbeController` / AutoConfiguration | PostgreSQL 打包 Smoke 与 MDM PostgreSQL IT；默认关闭、进入正式 JAR、HTTP `/internal/**` | 暂时保留，L3/L4 证据仍依赖；精确 ArchUnit 例外保持 | 业务 Repository 有等价 PostgreSQL契约测试且打包 Smoke 不再调用后，在业务 Slice 删除 |
| `MdmOutboxProbeService` / Controller | RocketMQ Outbox Smoke、MDM Outbox IT；默认关闭 | 暂时保留，不视为业务能力 | 真实业务 Outbox 用例替代 Phase 01 探针并完成消息验收 |
| `MdmSeataAtLocalParticipantService` / `MdmSeataAtProbeService` / Controller | Seata TC + 双数据库专项 Smoke；默认关闭 | 暂时保留，ADR-016 受控 PoC | Seata 采用/退出结论完成且专项验收有替代 fixture |
| `IntegrationSeataAtParticipantService` / Controller / Client | Seata 专项 Smoke；默认关闭 | 暂时保留 | 与 MDM Seata 探针同步删除，且不再有外部 CI 调用 |
| `IntegrationDomainEventConsumerConfiguration` | RocketMQ、Observability Smoke 使用 Phase 01 事件 | 暂时保留 | 真实领域事件消费者和 DLQ 验收替代 |
| `IntegrationIdempotencyProbeController` | S04 Redis Idempotency Smoke；默认关闭、唯一非测试 Key Factory 调用方 | 保留并改由独立、不经 Nacos/Gateway 的打包 Smoke 调用 | 首个真实幂等业务端点具备等价 L3/L4 故障测试 |
| `IntegrationMdmProbeController` / `MdmServiceProbeController` / `MdmServiceProbeClient` | Nacos、Tracing、Observability Stack；默认关闭 | 保留，用于独立 Nacos Discovery 证据 | 真实跨服务 Client 及 Gateway 路由验收覆盖注册、Feign、Correlation、发现中断 |
| `MomGatewayTechnicalProbeWebFilter` | 只允许上述两个 Gateway 探针在 Smoke 显式开启；默认 404 | 保留，未扩大路径 | 两个 Gateway 技术探针均移除后同步删除 |
| `application.yml` 的 `management.endpoints...bindings` | Messaging/运维技术验证，真实正式调用方证据不足 | 保持现状并登记 | S06/S08/P1.7 有部署调用方证据后决定保护或删除，不因 S04擅改 |
| 原 `nacos-redis-smoke.sh` | 一个脚本混合四种失败面 | 已删除并拆为三脚本 | 已关闭例外 |
| 五个 `*IntegrationTest` Testcontainers 类 | Surefire 会按 `*Test` 执行 Docker 测试 | 行为保持改名 `*IT`，由 Failsafe `verify` 执行 | 已关闭例外 |
| `MomServletResourceServerIntegrationTest` | 不使用 Docker的 Spring/MockMvc 组件测试 | 改名 `*Test`，仍由 Surefire 执行 | 已关闭例外 |

## 人工 Review

- Testcontainers 镜像多架构可用性、GitHub Runner 外部拉取异常和真实网络抖动不能由静态门禁证明。
- 技术探针是否仍有仓库外运维调用方需通过调用日志或 Chris 证据确认；当前仓库证据只授权“保留”，不授权删除。
- Spring Cloud Contract 和 JaCoCo 等工具只有出现真实消费者或可操作覆盖盲区后再评估。
