# P1.6 工程规范覆盖与缺口清单

- 基线 SHA：`5562e5ec1c4340dc2de0c5c80f54135df2ae6454`
- 盘点范围：根 `AGENTS.md`、工程规范、架构文档、ADR、CI 脚本、当前实现与测试
- 状态：S00 盘点基线；只冻结缺口归属，不替代 S01～S05 的正式规范

## 分类口径

| 分类 | 含义 |
|---|---|
| 已有权威规范 | 已有独立权威文档，当前只需维护 |
| AGENTS 已有 | 规则存在，但缺少便于引用的独立专题说明 |
| 文档无门禁 | 已有文档结论，缺少自动检查或验收矩阵 |
| 实现无规范 | 代码已形成做法，但尚未冻结为工程契约 |
| 完全缺失 | 当前没有足够文档或实现依据 |
| P1.6 暂不需要 | 不阻塞本阶段，明确推迟避免范围膨胀 |

## 覆盖与缺口

| # | 主题 | 当前分类与证据 | 缺口及后续 Slice |
|---:|---|---|---|
| 1 | 官方来源与版本升级 | **已有权威规范**：`official-source-policy.md` 及 JDK/Spring 四份规范 | S01 只引用，不重复定义 |
| 2 | Maven、依赖和模块收敛 | **文档无门禁**：根 POM、工程基线已有版本检查 | S01 补模块依赖、BOM 例外和收敛检查 |
| 3 | api/client/server 依赖方向 | **AGENTS 已有**，ArchUnit 仅覆盖部分模块 | S01 冻结依赖矩阵并补可执行门禁 |
| 4 | Java 包结构和应用分层 | **实现无规范**：IAM/MDM 已出现分层实践 | S01 定义包、入口和依赖方向 |
| 5 | Domain/Application/Infrastructure/Web 边界 | **AGENTS 已有**但分散 | S01 形成独立分层规范 |
| 6 | REST 路径和 API 版本 | **完全缺失** | S01 冻结路径、版本和兼容规则 |
| 7 | Request/Response DTO | **实现无规范** | S01 定义 DTO 与领域对象隔离 |
| 8 | Bean Validation 和输入规范化 | **实现无规范** | S01 定义校验层次、空白和大小写规范化 |
| 9 | 统一错误模型和异常映射 | **实现无规范**：IAM 已有局部错误模型 | S01 冻结跨模块错误信封与映射责任 |
| 10 | HTTP 状态码 | **实现无规范** | S01 建立状态码矩阵 |
| 11 | 分页、排序和过滤 | **完全缺失** | S01 定义稳定排序、白名单和上限 |
| 12 | 幂等请求和重复提交 | **AGENTS 已有**，Framework 有实现 | S01 定义 HTTP 契约；S02 衔接事务语义 |
| 13 | OpenAPI 或契约文档策略 | **完全缺失** | S01 决定生成、评审与兼容策略 |
| 14 | Schema、表、列、索引和注释 | **已有权威规范**：数据架构、ADR-004、AGENTS | S02 收敛命名、注释和迁移验收清单 |
| 15 | Entity、Mapper、Repository、Application Service | **AGENTS 已有**且 IAM/MDM 有实现 | S02 形成独立持久化边界规范 |
| 16 | 本地事务和跨服务一致性 | **已有权威规范**：ADR-014/015/016 与 AGENTS | S02 汇总选择树，不重写官方 Store |
| 17 | 乐观锁、并发更新和唯一约束 | **文档无门禁**：P1.5 有实现与测试 | S02 定义适用条件和并发验收 |
| 18 | 审计主体、审计事件和敏感数据 | **已有权威规范**：CurrentActor、安全基线、AGENTS | S02 区分数据审计与安全审计并补检查 |
| 19 | 配置、Profile、Secret 和安全默认值 | **文档无门禁**；S00 新增安全默认门禁 | S03 形成完整 Profile/Secret 规范并扩展 Fail Fast 测试 |
| 20 | OAuth2、OIDC、JWT、Session 和 Refresh | **已有权威规范**：ADR-019 与 P1.5 安全基线 | S03 规范化配置边界；S06/S07 后才允许实现调整 |
| 21 | Gateway 和 Resource Server | **已有权威规范**：安全架构与 P1.5 基线 | S03 固化端点、失败与配置约束 |
| 22 | OpenFeign、超时、重试和失败策略 | **AGENTS 已有**且配置存在 | S03 补独立调用方规范和重试禁区 |
| 23 | Redis Key、TTL、序列化和故障语义 | **AGENTS 已有**且 Framework 有实现 | S03 形成独立规范，保留 IAM revoked sid Fail Closed |
| 24 | 消息、Outbox、Inbox、DLQ | **已有权威规范**：ADR-015 与 AGENTS | S02 负责事务边界，S04 负责真实验收矩阵 |
| 25 | 日志、指标、Trace 和隐私 | **已有权威规范**：ADR-017/018、可观测性架构、AGENTS | S03 补敏感配置；S04 补自动验收 |
| 26 | 单元、集成、Smoke Test | **AGENTS 已有**，CI 已分层 | S04 建立测试金字塔和最小证据矩阵 |
| 27 | Surefire、Failsafe、Testcontainers | **部分门禁**：IAM Surefire 已受约束 | S04 冻结命名、生命周期与真实中间件边界 |
| 28 | CI 范围检测和最终验收 | **实现无完整规范**：已有 scope detector 与主 CI | S04 定义变更范围、必需 Job 和最终封板规则 |
| 29 | 国际化和 Locale | **完全缺失** | S05 形成设计基线；动态资源无调用方则推迟 S15 |
| 30 | UTC、用户时区和日期显示 | **AGENTS 已有**且数据架构部分覆盖 | S05 补 API、存储和展示转换规范 |
| 31 | 数字、小数、金额和计量单位 | **完全缺失** | S05 定义精度、舍入、序列化和单位边界 |
| 32 | 用户偏好 | **完全缺失** | S05 冻结所有权和默认/覆盖层级，S16 才实现 |
| 33 | ADR、弃用、兼容期和删除条件 | **文档无门禁**：ADR 索引存在，生命周期不完整 | S01 建立变更与删除证据要求；复杂演进推迟 P1.7 |
| 34 | Commit、Draft PR 和阶段治理 | **实现无规范**：历史阶段存在惯例 | S00 由 P1.6 计划和进度文件冻结；S04 纳入最终验收 |

## 优先级结论

1. S01～S05 均为规范与门禁 Slice，不提前建设 System 业务。
2. S03 只规范 IAM 安全配置和协议边界，不删除、合并或重写既有认证入口。
3. 现有 Spring Authorization Server JDBC Store、Refresh Rotation、Reuse Detection、Session 撤销与 JWT 校验均保持。
4. 国际化动态资源、复杂弃用平台和跨仓库契约生成若无真实调用方，分别推迟到 S15 或 P1.7。
