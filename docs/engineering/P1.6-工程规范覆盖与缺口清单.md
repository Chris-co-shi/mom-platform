# P1.6 工程规范覆盖与缺口清单

- 基线 SHA：`5562e5ec1c4340dc2de0c5c80f54135df2ae6454`
- 盘点范围：根 `AGENTS.md`、工程规范、架构文档、ADR、CI 脚本、当前实现与测试
- 状态：S01～S09 已完成；S10 In Progress / Blocked；S11 未开始

## 分类口径

| 分类 | 含义 |
|---|---|
| 已有权威规范 | 已有独立权威文档，当前只需维护 |
| AGENTS 已有 | 规则存在，但缺少便于引用的独立专题说明 |
| 文档无门禁 | 已有文档结论，缺少自动检查或验收矩阵 |
| 实现无规范 | 代码已形成做法，但尚未冻结为工程契约 |
| 完全缺失 | 当前没有足够文档或实现依据 |
| P1.6 暂不需要 | 不阻塞本阶段，明确推迟避免范围膨胀 |

## 覆盖与缺口

| # | 主题 | 当前分类与证据 | 缺口及后续 Slice |
|---:|---|---|---|
| 1 | 官方来源与版本升级 | **已有权威规范**：`official-source-policy.md` 及 JDK/Spring 规范 | S01 只引用，不重复定义 |
| 2 | Maven、依赖和模块收敛 | **已有权威规范与门禁**：模块分层规范、根 Enforcer、`mom-architecture-tests` | BOM 例外仍按官方来源政策 |
| 3 | api/client/server 依赖方向 | **已有权威规范与门禁**：POM XML 语义测试覆盖全 Reactor | 新模块自动纳入 |
| 4 | Java 包结构和应用分层 | **已有权威规范与渐进门禁** | 仍被 Smoke 使用的探针默认关闭并保留 |
| 5 | Domain/Application/Infrastructure/Web 边界 | **已有权威规范、ArchUnit 与 S09 行为保持拆分** | IAM Admin Controller 继续只依赖兼容 Facade；跨仓库协议回归归 S10 |
| 6 | REST 路径和 API 版本 | **已有权威规范**：HTTP API 契约规范 | 现有路径保持；破坏性版本独立评审 |
| 7 | Request/Response DTO | **已有权威规范**：DTO 与 Entity/Command 隔离 | IAM 内嵌 DTO/Map 在 S08/S09 渐进迁移 |
| 8 | Bean Validation 和输入规范化 | **已有权威规范**：协议/用例/领域三层校验 | 安全端点行为保持 |
| 9 | 统一错误模型和异常映射 | **已有目标规范与 S09 兼容证据**：Admin 内部使用 `IamAdminErrorResponse`，对外仍只有 `{code,message}` | OAuth2/OIDC、第一方 JSON、Spring Security 和 Gateway 错误继续分离 |
| 10 | HTTP 状态码 | **已有权威规范** | 旧状态码只在有兼容证据时迁移 |
| 11 | 分页、排序和过滤 | **已有权威规范** | IAM tie-breaker 与非法值语义归 S09 |
| 12 | 幂等请求和重复提交 | **已有权威规范并登记 Framework 差异** | 业务 Slice 实现完整语义 |
| 13 | OpenAPI 或契约文档策略 | **已有权威规范** | 有真实产物后接入兼容检查 |
| 14 | Schema、表、列、索引和注释 | **已有权威规范与门禁**：ADR-020、数据架构、持久化规范 | 复杂索引与迁移锁风险人工 Review |
| 15 | Entity、Mapper、Repository、Application Service | **已有权威规范、ArchUnit 与 S09 用例拆分** | Admin 保留兼容 Facade，事务归用户/授权/角色/Session/Client 服务 |
| 16 | 本地事务和跨服务一致性 | **已有权威规范** | 不重写 SAS 官方 Store |
| 17 | 乐观锁、并发更新和唯一约束 | **已有权威规范与基础门禁** | 业务竞争与索引有效性由业务 Slice 实测 |
| 18 | 审计主体、审计事件和敏感数据 | **已有权威规范** | 字段级保留期限后续确定 |
| 19 | 配置、Profile、Secret 和安全默认值 | **已有权威规范与渐进门禁**：ADR-021 | 部署值、TLS、RBAC 和 Secret 挂载归环境验收 |
| 20 | OAuth2、OIDC、JWT、Session 和 Refresh | **已有权威规范、S06 审计、ADR-024 与 S08 实现证据**：PC JSON、Mobile PKCE/OIDC、两类 Refresh Adapter 共享单一权威核心 | S08 已行为保持抽取认证用例、授权上下文、Claims/Token Issuer；S10 完成跨仓库回归 |
| 21 | 密码认证权威 | **ADR-024 Accepted**：密码只能进入 IAM；JSON Login 不是 Password Grant | 需要 S08/S10 架构测试和调用路径证据；禁止普通业务服务校验或持有密码 |
| 22 | 授权上下文与 Claims 权威 | **ADR-024 + S08 实现**：`IamAuthorizationContextLoader` 只读 IAM 权威数据，`IamJwtClaimsAssembler` 统一双通道核心 Claim | S10 继续做跨仓库 Claim 回归；不得接受客户端授权 Claims |
| 23 | Token 签发职责 | **ADR-024 + S08 实现**：标准端继续使用 SAS Generator，第一方使用复用同一 Encoder/JWK/Claims 的协议中立 `IamAccessTokenIssuer` | S10 验证完整签名元数据与协议 E2E |
| 24 | Password Grant 与内部 HTTP 回环 | **ADR-024 明确禁止且 S08 有行为测试**：Public Client 仅 Authorization Code/Refresh + PKCE，JSON 直接调用内部核心 | S10 保留跨仓库路径证据 |
| 25 | Gateway 和 Resource Server | **已有权威规范、Gateway 证据与 S10 Resource Server 撤销实现** | EG-02 已以协议中立端口、Servlet Filter、真实 Redis IT 关闭；总体封板仍受 EG-01/EG-03 阻塞 |
| 26 | OpenFeign、超时、重试和失败策略 | **已有权威规范与架构门禁** | 真实业务 Client 逐用例验证 |
| 27 | Redis Key、TTL、序列化和故障语义 | **已有权威规范** | 旧 Key 保持兼容 |
| 28 | 消息、Outbox、Inbox、DLQ | **已有权威规范** | 按业务 Slice 验证 |
| 29 | 日志、指标、Trace 和隐私 | **已有权威规范与验收边界** | 专项 Workflow 按范围执行 |
| 30 | 单元、集成、Smoke Test | **已有权威规范与门禁**：ADR-022 | 跨仓库 L6 进入 S10 |
| 31 | Surefire、Failsafe、Testcontainers | **已有权威规范与门禁** | 新 Docker 测试必须进入 Failsafe |
| 32 | CI 范围检测和最终验收 | **已有权威规范与正反例** | 最新 Head 必须区分 success/skipped/failure |
| 33 | 国际化和 Locale | **已有权威规范与轻量门禁**：ADR-023 | 动态资源无调用方时推迟 |
| 34 | UTC、用户时区和日期显示 | **已有权威规范与轻量门禁** | Factory 时区由 MDM 业务 Slice 建模 |
| 35 | 数字、小数、金额和计量单位 | **已有权威规范与轻量门禁** | 各业务 Slice 决定精度和换算版本 |
| 36 | 用户偏好 | **已有权威设计** | S16 有调用方后实现；不进入 IAM/JWT |
| 37 | ADR、弃用、兼容期和删除条件 | **已有权威规范、S06 门禁与 ADR-024 Accepted** | Web PKCE Package 状态为 `Compatibility / Future Migration Capability`；删除仍需证据和 Chris 批准 |
| 38 | Commit、Draft PR 和阶段治理 | **已有阶段规范**：P1.6 计划、进度与 Draft PR #33 | S10 In Progress / Blocked；S11 Not Started |

## S06 结论

1. `docs/engineering/P1.6-S06-IAM端点调用方与职责审计.md` 是调用方与职责权威证据。
2. 三个 PC Web 当前使用第一方 JSON；Mobile 使用系统浏览器 Authorization Code + PKCE S256。
3. 标准 Refresh Grant 与 JSON Refresh 共享 `IamSessionTokenService`，不能按重复实现删除。
4. S06 未修改认证协议、API、Token、Session、Refresh、Permission、Schema 或 Flyway。

## S07 Accepted 结论

1. ADR-024 状态为 Accepted；ADR-019 状态为 Superseded by ADR-024。
2. PC 第一方 JSON 与 Mobile Authorization Code/PKCE/OIDC 双通道长期并存。
3. JSON Login 是第一方协议 Adapter，不是 Password Grant；禁止标准或自定义密码 Grant。
4. 密码只由 IAM 接收和校验；普通业务服务、Gateway、System、业务 Resource Server、Mobile 和第三方服务不得校验或长期持有。
5. User、Client Policy、Role、Permission、Factory、Party、Session Context 和 Claims 只由 IAM 权威数据加载。
6. 两条 Adapter 共享账号认证、授权上下文、Session、Refresh、Claims、Token 签发、撤销和 Fail Closed 核心。
7. `IamFirstPartyLoginApplicationService`、`IamAuthorizationContextLoader`、`IamJwtClaimsAssembler`、`IamAccessTokenIssuer` 只是建议职责，尚未实现。
8. 可以在 S08 行为保持前提下评估 SAS `OAuth2TokenGenerator`、`JwtGenerator` 或等价组件；不得改变 Claim、签名、`kid`、TTL、Session、Refresh、撤销或外部协议。
9. 禁止 JSON Login 通过内部 HTTP 调用自身 `/oauth2/token`。
10. Web 标准 PKCE Package 产品状态已接受为 `Compatibility / Future Migration Capability`。
11. EG-01：目标决策关闭，Mobile Logout 具体协议/API/实现/E2E 未完成。
12. EG-02：保持 Open，归 S08/S10。
13. EG-03：架构决策关闭，正式 Client/Redirect 和 E2E 未完成。
14. EG-04：产品状态决策关闭，当前不删除、不接线、不描述为生产 Runtime。
15. 本次未修改生产代码、配置、脚本、Schema、Flyway、mom-web 或 mom-mobile。
16. S07 收口时状态：`S07: Completed；S08: Not Started`；当前总体状态以文首与实施进度为准。

## 优先级结论

1. S01～S05 为规范和门禁 Slice，没有提前建设 System 业务。
2. S06 完成真实调用方与职责审计，没有删除认证入口。
3. S07 接受长期双通道和统一认证后核心，不实施代码重构。
4. S08 已完成认证核心抽取；S09 已完成 Admin 用例拆分、错误兼容类型和 Token Adapter 行为证据。
5. S10 已关闭平台侧 EG-02，但 Mobile Logout、正式 Redirect/App Link、客户端完整测试及 L4/L6 证据仍阻塞；不得进入 System Server。
