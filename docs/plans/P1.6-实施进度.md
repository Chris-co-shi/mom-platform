# P1.6 实施进度

- 计划：[P1.6 IAM 与 System 平台治理计划](P1.6-IAM与System平台治理计划.md)
- 起始 Base SHA：`5562e5ec1c4340dc2de0c5c80f54135df2ae6454`
- 长期分支：`phase/p1.6-system-platform`
- 当前 Slice：S13
- 当前状态：`Completed`
- 下一 Slice：S14 `Not Started`

> Chris 已确认历史值仅用于无法从外网访问的本地内网开发环境，并接受当前不轮换、不清理历史、不批量失效 Session/Token 的决定。S00～S07 已按各自边界完成，长期 P1.6 PR 继续保持 Draft。
>
> Chris 于 2026-07-29 明确接受 ADR-024：PC 第一方 JSON 与 Mobile Authorization Code/PKCE/OIDC 双通道长期并存；两条 Adapter 共享 IAM 内部唯一的账号认证、授权上下文、Session、Refresh、JWT Claims、Token 签发和撤销核心。PC JSON 不是 Password Grant；密码和授权 Claims 只能由 IAM 权威处理。
>
> S08 已按 ADR-024 完成行为保持型认证核心抽取；S09 已完成 Admin 用例拆分、错误类型与 Token Adapter 证据化整理。S10 已完成 IAM 平台安全基线并关闭 EG-02。
>
> Chris 于 2026-07-29 明确批准路线纠偏：Mobile 服务端 Logout、正式 Client/Redirect/App Link、mom-mobile 完整检查、L4/L6 和 mom-infra 环境证据迁移到 S19-A 最终集成验收。它们仍为 Open/Deferred，不阻塞 S11，但继续阻塞 PR Ready、最终合并和生产发布。

## 里程碑进度

| Slice | 状态 | 当前结果或进入条件 |
|---|---|---|
| S00 | **Completed under accepted local-development risk** | 安全默认值、门禁、测试、规范缺口和阶段治理已交付；本地开发历史风险已由 Chris 接受 |
| S01 | **Completed** | 模块分层、包结构、HTTP API、DTO、异常、分页、幂等和渐进式架构门禁已交付 |
| S02 | **Completed** | ADR-020、持久化/事务/审计生命周期规范、精确历史例外和渐进门禁已交付；全 Reactor `clean verify` 通过 |
| S03 | **Completed** | ADR-021、配置/安全协议/OpenFeign/Redis 规范、精确历史清单、门禁与幂等 Key 修正已交付 |
| S04 | **Completed** | ADR-022、测试分层、Maven 生命周期、独立 Nacos/Redis Smoke 与增量 CI 证据保留已交付 |
| S05 | **Completed** | ADR-023、Locale/时区/Decimal/单位/偏好规范、跨仓库只读现状和轻量门禁已交付 |
| S06 | **Completed** | 固定 Ref 的端点、调用方、类职责、共享 Session/Refresh 核心和重复流程审计已完成 |
| S07 | **Completed** | ADR-024 Accepted；ADR-019 Superseded；双通道、IAM 权威、Claims/Token 职责和后续门禁已冻结 |
| S08 | **Completed** | 配置职责拆分、第一方认证核心抽取、双通道 Claim 与 Session/Refresh 行为回归已完成 |
| S09 | **Completed** | Admin 兼容 Facade、内聚用例服务、错误类型与 Token Adapter 特征证据已交付 |
| S10 | **Completed** | IAM 平台安全基线封板；EG-02 关闭；原跨仓库发布级阻塞项迁移至 S19-A |
| S11 | **Completed** | ADR-025 Accepted；方案 C 与七项决策已冻结；IAM 无生产数据迁移，`IamAdminService` 不迁移 |
| S12 | **Completed** | `mom-system-platform` api/client/server 技术骨架、最小配置、启动测试、POM 语义与 ArchUnit 门禁已完成；业务能力尚未开始 |
| S13 | **Completed** | GLOBAL/APPLICATION 类型化非敏感参数、mom_system/Flyway、MyBatis、审计乐观锁、安全 API 与本地全 Reactor 已通过 |
| S14～S18 | Not Started | 每个 Slice 仍需独立任务和停止门禁；未进入字典、偏好、目录或菜单能力 |
| S19-A | Not Started | 回收 Mobile Logout、正式 Redirect/App Link、客户端完整检查、L4/L6 和 mom-infra 证据 |
| S19 | Not Started | 仅在 S19-A、必要轮换和最终 CI 全绿后执行最终封板 |

## 当前禁止项

- 未执行认证协议迁移；S08 仅完成行为保持型 IAM 内部重构。
- `mom-system-platform` 正在实施 S13 参数能力；Dictionary、Preference、Catalog、Menu、Navigation 仍未创建。
- 未修改 OAuth2/OIDC、PKCE、Token、Session、Refresh、Permission、公开 API、Schema 或 Flyway。
- 未修改 mom-web、mom-mobile。
- 未新增 Password Grant 或自定义密码 Grant。
- 未让 JSON Login 内部 HTTP 调用 `/oauth2/token`。
- 已抽取唯一 `IamJwtClaimsAssembler` 与复用现有 JWK/Encoder 的 `IamAccessTokenIssuer`，未创建第二套 Token 核心。
- EG-01、EG-03、mom-mobile 完整检查及 L4/L6 不得描述为已关闭或已通过。
- PR #33 继续保持 Open、Draft、未合并；S19-A 前不得转 Ready 或合并。

## S00 核对表

| 项目 | 状态 |
|---|---|
| 分支与 Base 无分叉 | Verified |
| 环境中立配置与危险默认项清理 | Implemented |
| IAM 测试密钥移出正式主资源 | Implemented |
| `secure-defaults` 正反例与 CI 门禁 | Implemented |
| 工程规范缺口清单 | Implemented |
| 历史凭据使用范围 | Verified：仅本地内网开发环境，无法从外网访问 |
| 凭据轮换 | Accepted Risk / Not Performed |
| Git 历史清理 | Accepted Risk / Not Performed |
| Session、Refresh Token、Access Token 批量失效 | Accepted Risk / Not Performed |
| PR Ready / Merge | P1.6 长期 PR 继续保持 Draft，不在 S00 合并 |

## S02 核对表

| 项目 | 状态 |
|---|---|
| `mom_<bounded-context>` Schema 命名与 ADR-020 | Accepted / Documented |
| Schema Rename、业务 Migration、生产持久化行为 | Not Performed |
| Flyway/MyBatis/Entity 轻量门禁 | Passed |
| 根 Reactor `clean verify` | Passed |
| S03（S02 收口时） | Not Started |

## S03 核对表

| 项目 | 状态 |
|---|---|
| ADR-021、Profile/Secret/Nacos 动态刷新边界 | Accepted / Documented |
| IAM SecurityFilterChain 与协议矩阵 | Documented / Behavior Preserved |
| Gateway、Resource Server、Actuator、OpenFeign 与 Redis 规范 | Completed |
| 运行时安全门禁 | Passed |
| OAuth2/OIDC、认证入口、Token/Session/Refresh/Permission | Not Changed |
| Schema、Flyway、Gateway Route、Redis 命令/TTL/Lua/故障策略 | Not Changed |
| S04（S03 收口时） | Not Started |

## S05 核对表

| 项目 | 状态 |
|---|---|
| ADR-023、`zh-CN`/`en-US`、Locale 选择与回退 | Accepted / Documented |
| UTC、用户/Factory/设备时区、业务日期与 DST | Completed |
| Decimal String、金额、货币、比例与舍入 | Completed |
| MDM 单位所有权、量纲、换算与历史快照 | Completed |
| System 用户偏好所有权、S15/S16 进入条件 | Completed |
| mom-web / mom-mobile | Read-only evidence collected；Not Changed |
| S06（S05 收口时） | Not Started |

## S06 核对表

| 项目 | 状态 |
|---|---|
| 权威审计文档 | Completed：`docs/engineering/P1.6-S06-IAM端点调用方与职责审计.md` |
| mom-platform 固定 Ref | `cabe48c2fe9a7ffced081fbda6c4c68346143182` |
| mom-web 固定 Ref | `40106d3fb3ab3c6d52bb0b079a36acbdc1477cef`；Read-only |
| mom-mobile 固定 Ref | `ab357ee2fcb1177e6c32806dfa7c32a387d4a6cc`；Read-only |
| PC 第一方 JSON 调用方 | Verified：Admin、Supplier、Customer |
| Mobile PKCE/OIDC 调用方 | Verified：系统浏览器 Authorization Code + PKCE S256 |
| Session/Refresh 权威核心 | Verified：共享 `IamSessionTokenService` |
| 可立即删除的认证入口 | None |
| 保留架构缺口 | EG-01～EG-04 进入 S07/S08/S10/S19-A |
| 生产代码/API/Schema/Flyway | Not Changed |
| S07（S06 收口时） | Not Started |

## S07 核对表

| 项目 | 状态 |
|---|---|
| ADR | `docs/adr/ADR-024-PC-JSON与Mobile-PKCE-OIDC双通道.md` |
| ADR-024 | Accepted；Chris 于 2026-07-29 明确批准 |
| ADR-019 | Superseded by ADR-024；保留历史价值 |
| 双通道 | PC JSON + Mobile Authorization Code/PKCE/OIDC 长期并存 |
| 第一方 JSON 定位 | MOM 第一方协议 Adapter；不是 Password Grant |
| Password Grant | 禁止 `grant_type=password` 和自定义密码 Grant |
| 密码认证权威 | 仅 IAM；普通业务服务、Gateway、System、业务 Resource Server、Mobile 不得校验或长期持有密码 |
| 授权上下文权威 | User、Client Policy、Role、Permission、Factory、Party、Claims 仅由 IAM 权威数据加载 |
| 共享核心 | 账号认证、授权上下文、Session、Refresh、Claims、Token 签发、撤销 |
| Token 组件方向 | 可在 S08 行为保持前提下评估 SAS `OAuth2TokenGenerator`/`JwtGenerator` 或等价内部组件 |
| 内部 HTTP 回环 | 禁止 JSON Login 调用自身 `/oauth2/token` |
| Web PKCE Package | Accepted 状态：`Compatibility / Future Migration Capability` |
| EG-01 | 目标决策已关闭；具体 Mobile Logout 实现/E2E 延迟至 S19-A |
| EG-02 | 已在 S10 关闭 |
| EG-03 | 架构决策已关闭；正式 Client/Redirect 和 E2E 延迟至 S19-A |
| EG-04 | 产品状态决策已关闭；当前不删除、不接线、不描述为生产 Runtime |
| 生产代码、配置、脚本、Schema、Flyway | Not Changed |
| mom-web / mom-mobile | Not Changed |
| S08 | Completed；行为保持配置拆分、第一方登录应用服务、Context Loader、Claims Assembler 与 Token Issuer 已交付 |
| S07 收口状态 | `S07: Completed；S08: Not Started` |

## S09 核对表

| 项目 | 状态 |
|---|---|
| Admin 职责 | 兼容 Facade + 六个内聚应用服务 |
| 事务、锁、Version、Session 撤销与审计 | Behavior Preserved / Characterized |
| Admin HTTP 契约 | Path、Method、DTO、Status、Permission、分页默认值未变 |
| Admin Error | `IamAdminErrorResponse(code,message)`；JSON 两字段未变 |
| Token Adapter | 保留全部 Adapter；共享 Rotation/Issuer；标准 OAuth2 响应未变 |
| 历史 `a925107…` | 未读取、未复用、未 cherry-pick |
| EG-02 | 已在 S10 关闭 |
| Schema/Flyway/Gateway/Resource Server | S09 未修改 |
| S10 | Completed |

## S10 核对表

| 项目 | 状态 |
|---|---|
| 固定 Ref | platform 起始 `7456d009…`；web `40106d3f…`；mobile `ab357ee2…`；infra Evidence Gap |
| PC 三应用第一方 Runtime | L1/L2/L5 Passed；真实账号 L4 Deferred to S19-A |
| Mobile PKCE/OIDC | auth 8 tests Passed；真机/App Link L6 Deferred to S19-A |
| EG-01 Mobile Logout | Open / Deferred to S19-A：当前只清理本地状态 |
| EG-02 Resource Server revoked sid | Closed：协议中立端口、Servlet Filter、Redis IT |
| EG-03 Redirect URI | Open / Deferred to S19-A：本地 H5 默认不匹配，正式值 Pending |
| EG-04 Web PKCE Package | Closed：保留但未接入三个 PC Runtime |
| mom-mobile 完整检查 | Deferred to S19-A：离线测试无法解析 `@/auth`；只读未修改 |
| S10 平台安全基线 | Completed |
| S11 | Completed；ADR-025 Accepted |
| S12（S10 收口时） | Not Started |

## S19-A 延迟集成清单

| 编号 | 事项 | 当前状态 | 最终门禁 |
|---|---|---|---|
| DI-01 | Mobile Logout 调用 IAM 服务端撤销 Session | Open / Deferred | 必须验证 Session、Refresh、revoked sid 和本地清理 |
| DI-02 | Test/Staging/Production Client ID 与精确 Redirect URI | Open / Deferred | 禁止通配，IAM/Mobile/部署三方一致 |
| DI-03 | Android HTTPS App Link 与 `assetlinks.json` | Open / Deferred | 域名、Package、签名 SHA-256 和真机回调通过 |
| DI-04 | mom-mobile 完整 `pnpm run check` | Open / Deferred | 不得跳过失败测试 |
| DI-05 | IAM/Gateway/Resource Server/PostgreSQL/Redis L4 E2E | Open / Deferred | 打包应用真实 HTTP 全绿 |
| DI-06 | Android 真机、系统浏览器、强杀恢复 L6 | Open / Deferred | 真实设备与恢复路径通过 |
| DI-07 | mom-infra Ingress、Service、NetworkPolicy 固定版本证据 | Evidence Gap / Deferred | 外部暴露和绕行边界已验证 |

## S11 核对表

| 项目 | 状态 |
|---|---|
| ADR-025 | Accepted；2026-07-29；决策人 Chris；方案 C 与七项决策已冻结 |
| Person / Employee | V1 MDM 唯一权威；IAM 仅 Reference；未来 HR 生命周期另立 ADR；当前不改 `employee_no` |
| Party | MDM Own 核心身份；采购/SRM Own 供应商采购关系；销售/CRM Own 客户销售关系；未来业务域均 Not Implemented；IAM 只 Own Party Binding |
| Application Catalog | System 独立 `applicationCode`，可选引用 IAM `clientId`；不复制 OAuth 安全配置 |
| Display Name | IAM `displayName` 是独立账号显示标签；MDM `personName` 是人员姓名；不是同一权威字段，不持续双向同步 |
| 多 Party | P1.6 一账号最多一个 Party；未来真实需求另立 ADR |
| Dynamic I18n | 有真实调用方才执行 S15，否则 Deferred 且不建表/API/缓存/后台页面 |
| Audit Projection | 暂缓统一 Audit Center 与跨域 Projection；原始审计保留各权威域 |
| IAM 真实生产数据迁移 | No current production migration required |
| `IamAdminService` | 保留 IAM，不迁移 |
| Permission / Warehouse / Equipment | IAM / WMS / EAM 各自 Own |
| System 参数 / Preference | 参数禁止 Secret；Preference 不参与 Authorization |
| `mom-system-platform` / S12（S11 收口时） | 未创建 / Not Started |

## S12 核对表

| 项目 | 状态 |
|---|---|
| 起始 Head | `8b079eafaac52848bfaae68305d3bd4818b612fb` |
| 根 Reactor | 已在 `mom-iam-platform` 与 `mom-mdm-platform` 之间注册 `mom-system-platform` |
| 模块 | `mom-system-api` / `mom-system-client` / `mom-system-server` |
| API | 空契约骨架；仅 `package-info.java` |
| Client | 空调用骨架；只依赖 System API 与 `mom-openfeign` |
| Server | 最小启动类、环境中立配置、web/application/domain/infrastructure 包边界 |
| Server 依赖 | System API、WebMVC、Tracing、Metrics、Nacos Discovery、`mom-test`（test） |
| 禁止依赖 | IAM/其他 Server、Security Runtime、Data、Flyway、数据库、Redis、MQ、Outbox、Seata 均未引入 |
| POM 语义门禁 | 根注册、聚合精确模块、API/Client/Server 白名单和 `mom-iam-server` 负例 Fixture |
| ArchUnit | System 分层方向与 IAM 内部实现禁区已纳入通用门禁 |
| 业务能力 | 参数、字典、Preference、Application Catalog、Menu、Navigation、Dynamic I18n、Audit Projection 均未实现 |
| 数据资源 | 无 Entity、Mapper、Repository、Schema、SQL、Flyway |
| 阶段结论 | 技术骨架与依赖门禁已完成；业务能力尚未开始 |

## S13 完成状态

S13 已在当前本地工作区完成 GLOBAL/APPLICATION 类型化非敏感参数，领域规则、五种值类型规范化、`mom_system` Flyway、MyBatis、审计、乐观锁、管理/解析 API、安全测试、PostgreSQL Testcontainers、Changed Verification 与根 Reactor 均通过。S14 保持 `Not Started`，不得自动进入字典、Preference、Application Catalog、Menu、Navigation、Dynamic I18n 或 Audit Projection。
