# P1.5：认证与授权闭环实施进度

- 权威设计：[P1.5 认证与授权设计基线](../security/P1.5-认证与授权设计基线.md)
- 完整计划：[P1.5 认证与授权闭环实施计划](P1.5-认证与授权闭环计划.md)
- 当前状态：`Completed / Merged`
- 更新时间：2026-07-23

| Slice | 状态 | 结果 |
|---|---|---|
| S00 | **Completed / Merged** | 三仓库设计基线、ADR、职责矩阵和 Definition of Done 已合并 |
| S01 | **Completed / Merged** | CurrentActor、显式 SYSTEM、MyBatis-Plus 强制审计、受控更新路径和乐观锁已合并 |
| S02 | **Completed / Merged** | IAM Schema、正式 Flyway、领域模型、初始化目录、PostgreSQL 测试和中文注释检查已合并 |
| S03 | **Completed / Merged** | Authorization Server、OIDC、Authorization Code + PKCE S256、账号认证、四个 Public Client、首次改密、JDBC Store 与 JWK/JWT 基础已随提前合并的阶段 PR #20 进入 main |
| S04 | **Completed / Merged** | 有效 RBAC/Permission、Factory/Party Scope、JWT 业务 Claims、Bearer `/api/iam/me`、Current Factory 与统一 404 基础契约已完成并通过全量 CI |
| S05 | **Completed / Merged** | Web/Mobile 绝对 Session、Opaque Refresh、HMAC 摘要、单 ACTIVE Rotation、重放检测、COMPROMISED、Redis revoked sid 与 Fail Closed 已完成并通过全量 CI |
| S06 | **Completed / Merged** | Gateway 与业务 Resource Server 的 JWT/Issuer/Audience、Client 路由、revoked sid、Header 清理、Bearer 转发、Permission/Factory/Party 与 404 防枚举已完成并通过全量 CI |
| S07 | **Completed / Merged / Contract Hardened** | IAM 管理 API 与 S09 联调契约加固已完成；授权读取返回父聚合版本，五类关系替换强制校验客户端版本，并通过 PostgreSQL 并发/回滚 E2E、基础设施 Smoke 与全量 CI |
| S08 | **Completed / Merged** | `mom-web` 三应用 Web Auth Runtime 已进入 main，并在长期 Draft PR Exact SHA 上通过 frozen install、validate、typecheck、7 项认证测试与三应用构建 |
| S09 | **Completed / Merged** | MOM Admin 用户、角色、Scope、Session、安全审计与 OAuth Client 管理页面已进入 `mom-web` main |
| S10 | **Completed / Merged** | Supplier/Customer Portal 主体固定、Factory Scope、业务入口与错误处理已进入 `mom-web` main |
| S11 | **Completed / Merged** | `mom-mobile` Auth Runtime、PKCE/OIDC、冷启动、Single Flight、安全存储 Adapter 与离线身份门禁完成；真实联调触发 `/api/iam/me` 增补 `sid`、`mobileAccessEnabled` |
| S12 | **Completed / Merged** | 三仓库安全 E2E、CI、敏感信息扫描、文档一致性与 P1.5 封板完成 |

## S03～S07 已交付

- Authorization Server、OIDC、Authorization Code + PKCE S256、四个 Public Client 与数据库账号认证。
- 有效 Role/Permission、Factory/Party Scope、JWT 业务 Claims、Bearer `/api/iam/me`、Current Factory 与统一 404 基础契约。
- Web 8 小时、Mobile 12 小时绝对 Session；Opaque Refresh、HMAC 摘要、单 ACTIVE Rotation、重放检测、COMPROMISED、Redis revoked sid 与 Fail Closed。
- Gateway 与业务 Resource Server 双重 JWT 校验、Client 路由隔离、伪造身份 Header 清理、Bearer 转发、Permission/Factory/Party 最终授权和 404 防枚举。
- `/api/iam/admin/**` 提供用户、角色、Permission、Factory Scope、Mobile Access、Session、安全审计、OAuth Client 与外部 Party Binding 管理能力。
- 用户授权与角色 Permission 读取分别返回 `userVersion`、`roleVersion`；Role、Factory、Mobile、Party 与 Permission 全量替换命令必须提交读取版本，成功后父聚合版本原子加一并返回完整最新快照。
- 过期读取版本稳定返回 HTTP 409 `stale_version`，关系、父版本和成功审计在同一 PostgreSQL 事务中保持零副作用；最后一个 `PLATFORM_ADMIN` 与内置角色保护继续有效。
- 高风险写操作逐项校验 Permission，管理员自我禁用/删除、最后一个有效 `PLATFORM_ADMIN`、跨 `user_type` 角色分配和外部 Factory Scope 均 Fail Closed。
- 用户或 Client 禁用会撤销 PostgreSQL Session/Refresh 状态并写入 Redis revoked sid。

## S08 已完成

- `mom-web` 实现 `@mom/auth`、`@mom/access`、`@mom/api-client` 与三应用独立 Public Client。
- Access、Refresh、ID Token 仅内存保存；`sessionStorage` 仅保存一次性 PKCE 事务并在使用后删除。
- Callback 校验 state、nonce、RS256、Issuer、Audience、Expiry，并清理地址栏协议参数。
- 401 使用 Single Flight，每业务请求最多重试一次；403 不刷新。
- `/api/iam/me` 是完整权限上下文来源；当前 Factory 偏好重新校验；外部门户不提供 Party 切换。
- Web CI 已加入 concurrency 与 TypeScript 失败诊断；TypeScript 6、Web Crypto、真实认证测试和三应用构建全部通过。

## S09～S11 已完成

- `mom-web` S09/S10 已完成 MOM Admin 与 Supplier/Customer Portal，并由提前合并的 PR #4 进入 main，最终功能 Head 为 `07172e2a5acecf7e27a1bf0f1a9a4c089be3496d`。
- `mom-mobile` S11 已完成唯一 `mom-mobile-pda`、系统浏览器 PKCE S256/OIDC、Access 内存、Refresh 安全存储 Adapter、冷启动、Single Flight、401 单次重试、403 不刷新和 UNKNOWN Fail Closed。
- 离线命令完整记录 userId、sid、clientId、factoryId、partyType/partyId、requiredPermission、createdAt、idempotencyKey 与 correlationId；跨用户、Session、Factory、Permission 和撤销状态全部 Fail Closed。
- S11 真实联调确认 `/api/iam/me` 缺少 Session 与 Mobile Access 权威字段；仅向后兼容增加 `sid`、`mobileAccessEnabled`，不改变 S07 已冻结管理契约或最终授权语义。

## 最终封板证据

- `mom-platform` main：`9d38d0c4c29d257b7ba88e645c85454f722fc18a`；P1.5 Head `863cd0250c59dab387f7d9ff992972b9605bfb54` 的 Main、Messaging、Seata、Observability、Observability Stack CI 全绿。
- `mom-web` main：`cca3473541c37f98af62c4aa24f5c9d6e6e1ec76`；P1.5 Head `60c5ac51ae7f570a5c4a7f91e03cb8b91fa7455d` 的 Web CI 全绿。
- `mom-mobile` main：`4f06b855f05443768ed3b0d19832c87d9df3dad8`；P1.5 Head `2424ad09c639faf498c1cbac00b352fd173b89b7` 的 Mobile CI 全绿。
- Android Keystore、HTTPS App Link 与真机强杀恢复登记为 Phase 02 Mobile 正式联调前置验收项，不影响 P1.5 Completed / Merged。
