# P1.5：认证与授权闭环实施进度

- 权威设计：[P1.5 认证与授权设计基线](../security/P1.5-认证与授权设计基线.md)
- 完整计划：[P1.5 认证与授权闭环实施计划](P1.5-认证与授权闭环计划.md)
- 当前 Slice：`S01`
- 更新时间：2026-07-20

| Slice | 状态 | 结果 |
|---|---|---|
| S00 | **Completed** | 三仓库设计基线、ADR、职责矩阵和 Definition of Done 已合并 |
| S01 | **Completed in branch / In Review** | CurrentActor、显式 SYSTEM、MyBatis-Plus 强制审计、受控更新路径和乐观锁已实现并通过 CI |
| S02 | **Pending** | IAM 数据库与领域模型，等待 S01 Review/合并后开始 |
| S03～S12 | **Pending** | 按完整计划顺序执行 |

## S01 已交付

- `mom-core`：`AuditActor`、`ActorType`、`CurrentActorProvider`、`AuditActorMissingException`、`AuditContextExecutor`。
- `mom-security`：显式 Actor 优先、Spring Security 用户解析、匿名身份排除。
- `mom-data`：服务端强制审计填充、UTC Clock、实体分层、Wrapper-only Update 拒绝、单一乐观锁插件。
- 测试：上下文嵌套/异常/线程复用、JWT Claim、INSERT/UPDATE/批量/自定义 SQL、缺少 Actor、SYSTEM Actor、乐观锁和既有 PostgreSQL 基线。
- 文档：[CurrentActor 与数据审计基础](../architecture/CurrentActor与数据审计.md)。

S01 未实现 IAM 表、OAuth/OIDC、JWT 签发、RBAC、Factory/Party Scope、Session、Gateway Resource Server 或前端登录。
